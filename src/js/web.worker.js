// import parse from 'csv-parse/lib/sync';

// self.addEventListener('message', (e) => {
//   const records = parse(e.data, {
//     columns: true,
//     skip_empty_lines: true,
//     skip_lines_with_empty_values: true,
//   });
//   self.postMessage(records);
// });
